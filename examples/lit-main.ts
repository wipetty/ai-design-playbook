// Example: Lit entry point
import './app-root';
import './index.css';

// Web Components are self-registering via @customElement decorator
// Just import the component file and it registers itself
// The <app-root></app-root> tag will be available in HTML
